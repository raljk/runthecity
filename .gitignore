# Publish only the game page, its two sound packs and the scripts that make them. Everything else in this folder stays local (notes, raw art, the API key file).
/*
!/index.html
!/sound-fx.bin
!/sound-table.bin
# the scripts that make the sound, but not the audio they make or the audition pages
!/sound/
/sound/*
!/sound/*.py
!/sound/README.md
!/.gitignore
